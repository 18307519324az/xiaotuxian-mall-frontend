# 环境变量示例 — 小兔鲜前端

## 说明

复制此文件为 `.env` 并根据需要修改。

## 使用方式

```bash
# Windows CMD
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

## 环境变量列表

# API 请求基础路径（开发环境使用 /api 代理到 Mock 服务）
# 生产环境需要改为真实 API 地址
# VUE_APP_API_BASE_URL=/api

# Mock/Real 服务切换（设为 "real" 启用真实微服务，默认不设置即使用 Mock）
# VUE_APP_ADDRESS_SERVICE_MODE=mock
# VUE_APP_CATEGORY_SERVICE_MODE=mock
# VUE_APP_GOODS_SERVICE_MODE=mock
# VUE_APP_BRAND_SERVICE_MODE=mock
# VUE_APP_TOPIC_SERVICE_MODE=mock
# VUE_APP_GOODS_LIST_SERVICE_MODE=mock
